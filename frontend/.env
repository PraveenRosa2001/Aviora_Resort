# Base URL of the ASP.NET Core Web API.
# Read the exact port from Properties/launchSettings.json in the backend project.
# The value must end with /api because every controller route starts with api/.
AVIORA_API_BASE_URL=https://localhost:7215/api
VITE_API_BASE_URL=https://localhost:7215/api